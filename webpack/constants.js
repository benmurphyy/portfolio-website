const path = require('path');
/**
 * Defines and exports constants that are used by multiple webpack config files.
 */
module.exports = {
  // absolute path of the root directory of the project
  rootDir: path.resolve(__dirname, '..'),
};
